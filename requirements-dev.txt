# Development and Testing Dependencies
# Install production dependencies first: pip install -r requirements.txt
# Then install dev dependencies: pip install -r requirements-dev.txt

# Testing Framework
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0

# HTTP Mocking (use respx instead of httpx-mock for Python 3.12 compatibility)
respx>=0.20.0

# Code Quality
black>=23.0.0
isort>=5.12.0
flake8>=6.0.0
mypy>=1.5.0

# Development Tools
ipython>=8.0.0
jupyter>=1.0.0

# Pre-commit hooks
pre-commit>=3.0.0